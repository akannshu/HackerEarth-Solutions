#include<bits/stdc++.h>
using namespace std;

int main()
{
	int freq[26]={0};
	string s;
	cin>>s;
	for(int i=0; i<s.length();i++)
		if(freq[s[i]-'a']==0)
		{
			freq[s[i]-'a']++;
			cout<<s[i];
		}	
	return 0;
}
